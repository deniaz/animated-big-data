var GraphBuilder = (function() {
	_nodes = [];

	function add(el) {
		_nodes.push(el);
	}

	function getNodes() {}

	function getLinks() {}

	function buildFromArray(arr) {

	}

	return {
		add: add,
		getNodes: getNodes,
		getLinks: getLinks,
		buildFromArray: buildFromArray,
	};
})();